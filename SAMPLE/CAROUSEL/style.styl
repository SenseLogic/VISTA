@require "../../vista.styl"

// ~~

MakeContainerCentered( 960 * Pxm, {}, {} );
MakeCarousel( 3, 3s, 0.5s );
MakeCarousel( 4, 3s, 0.5s );
MakeCarousel( 5, 3s, 0.5s );
MakeCarousel( 6, 3s, 0.5s );

// ~~

html
{
    background: #FFFFFF;
}
