@require "../../vista.styl"

// ~~

Pt = 0.0625rem;

// ~~

MakeRemSize( 16px, {}, {}, 0 );
MakeWidth( 480, Pt, "pt", {}, 0 );
MakeHeight( 80, Pt, "pt", {}, 0 );
MakeMarginLateral( 20, Pt, "pt", {}, 0 );
MakeMarginTop( 10, Pt, "pt", {}, 0 );
MakeMarginTop( 20, Pt, "pt", {}, 0 );
MakeMarginTop( 40, Pt, "pt", {}, 0 );
MakeInput( 10 * Pt, 5 * Pt );
MakeButton( 10 * Pt );
MakeSelect( 10 * Pt );

// ~~

html
{
   background-color: #FFFFFF;
}

body
{
    font-size: 16 * Pt;
}

body *
{
    border: 1px dotted #CCCCCC;
}

// ~~

.content
{
    max-width: 1200 * Pt;
    margin-left: auto;
    margin-right: auto;
}

.header
{
    background-color: #888888;
    color: white;
}

.main
{
    background-color: #EEEEEE;
    color: black;
}
