MakeMarginAuto( media_query_name_list = {}, prefix =, suffix = )

    .{prefix}margin-auto{suffix}
    {
        margin: auto;
    }

    for media_query_name in media_query_name_list
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .{prefix}margin-auto-{media_query_name}{suffix}
            {
                margin: auto;
            }
        }
    }

// ~~

MakeMarginValue( size, unit_value, unit_name, media_query_name_list = {}, prefix =, suffix = )

    .{prefix}margin-{size}{unit_name}{suffix}
    {
        margin: size * unit_value;
    }

    for media_query_name in media_query_name_list
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .{prefix}margin-{size}{unit_name}-{media_query_name}{suffix}
            {
                margin: size * unit_value;
            }
        }
    }

// ~~

MakeMarginValueRange( start, step, count, unit_value, unit_name, media_query_name_list = {}, prefix =, suffix = )

    for index in 0 .. count - 1
    {
        size = start + index * step;

        MakeMarginValue( size, unit_value, unit_name, media_query_name_list, prefix, suffix );
    }

// ~~

MakeMarginVerticalAuto( media_query_name_list = {}, prefix =, suffix = )

    .{prefix}margin-vertical-auto{suffix}
    {
        margin-top: auto;
        margin-bottom: auto;
    }

    for media_query_name in media_query_name_list
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .{prefix}margin-vertical-auto-{media_query_name}{suffix}
            {
                margin-top: auto;
                margin-bottom: auto;
            }
        }
    }

// ~~

MakeMarginVerticalValue( size, unit_value, unit_name, media_query_name_list = {}, prefix =, suffix = )

    .{prefix}margin-vertical-{size}{unit_name}{suffix}
    {
        margin-top: size * unit_value;
        margin-bottom: size * unit_value;
    }

    for media_query_name in media_query_name_list
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .{prefix}margin-vertical-{size}{unit_name}-{media_query_name}{suffix}
            {
                margin-top: size * unit_value;
                margin-bottom: size * unit_value;
            }
        }
    }

// ~~

MakeMarginVerticalValueRange( start, step, count, unit_value, unit_name, media_query_name_list = {}, prefix =, suffix = )

    for index in 0 .. count - 1
    {
        size = start + index * step;

        MakeMarginVerticalValue( size, unit_value, unit_name, media_query_name_list, prefix, suffix );
    }

// ~~

MakeMarginLateralAuto( media_query_name_list = {}, prefix =, suffix = )

    .{prefix}margin-lateral-auto{suffix}
    {
        margin-left: auto;
        margin-right: auto;
    }

    for media_query_name in media_query_name_list
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .{prefix}margin-lateral-auto-{media_query_name}{suffix}
            {
                margin-left: auto;
                margin-right: auto;
            }
        }
    }

// ~~

MakeMarginLateralValue( size, unit_value, unit_name, media_query_name_list = {}, prefix =, suffix = )

    .{prefix}margin-lateral-{size}{unit_name}{suffix}
    {
        margin-left: size * unit_value;
        margin-right: size * unit_value;
    }

    for media_query_name in media_query_name_list
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .{prefix}margin-lateral-{size}{unit_name}-{media_query_name}{suffix}
            {
                margin-left: size * unit_value;
                margin-right: size * unit_value;
            }
        }
    }

// ~~

MakeMarginLateralValueRange( start, step, count, unit_value, unit_name, media_query_name_list = {}, prefix =, suffix = )

    for index in 0 .. count - 1
    {
        size = start + index * step;

        MakeMarginLateralValue( size, unit_value, unit_name, media_query_name_list, prefix, suffix );
    }

// ~~

MakeMarginTopAuto( media_query_name_list = {}, prefix =, suffix = )

    .{prefix}margin-top-auto{suffix}
    {
        margin-top: auto;
    }

    // ~~

    for media_query_name in media_query_name_list
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .{prefix}margin-top-auto-{media_query_name}{suffix}
            {
                margin-top: auto;
            }
        }
    }

// ~~

MakeMarginTopValue( size, unit_value, unit_name, media_query_name_list = {}, prefix =, suffix = )

    .{prefix}margin-top-{size}{unit_name}{suffix}
    {
        margin-top: size * unit_value;
    }

    for media_query_name in media_query_name_list
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .{prefix}margin-top-{size}{unit_name}-{media_query_name}{suffix}
            {
                margin-top: size * unit_value;
            }
        }
    }

// ~~

MakeMarginTopValueRange( start, step, count, unit_value, unit_name, media_query_name_list = {}, prefix =, suffix = )

    for index in 0 .. count - 1
    {
        size = start + index * step;

        MakeMarginTopValue( size, unit_value, unit_name, media_query_name_list, prefix, suffix );
    }

// ~~

MakeMarginBottomAuto( media_query_name_list = {}, prefix =, suffix = )

    .{prefix}margin-bottom-auto{suffix}
    {
        margin-bottom: auto;
    }

    for media_query_name in media_query_name_list
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .{prefix}margin-bottom-auto-{media_query_name}{suffix}
            {
                margin-bottom: auto;
            }
        }
    }

// ~~

MakeMarginBottomValue( size, unit_value, unit_name, media_query_name_list = {}, prefix =, suffix = )

    .{prefix}margin-bottom-{size}{unit_name}{suffix}
    {
        margin-bottom: size * unit_value;
    }

    for media_query_name in media_query_name_list
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .{prefix}margin-bottom-{size}{unit_name}-{media_query_name}{suffix}
            {
                margin-bottom: size * unit_value;
            }
        }
    }

// ~~

MakeMarginBottomValueRange( start, step, count, unit_value, unit_name, media_query_name_list = {}, prefix =, suffix = )

    for index in 0 .. count - 1
    {
        size = start + index * step;

        MakeMarginBottomValue( size, unit_value, unit_name, media_query_name_list, prefix, suffix );
    }

// ~~

MakeMarginLeftAuto( media_query_name_list = {}, prefix =, suffix = )

    .{prefix}margin-left-auto{suffix}
    {
        margin-left: auto;
    }

    for media_query_name in media_query_name_list
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .{prefix}margin-left-auto-{media_query_name}{suffix}
            {
                margin-left: auto;
            }
        }
    }

// ~~

MakeMarginLeftValue( size, unit_value, unit_name, media_query_name_list = {}, prefix =, suffix = )

    .{prefix}margin-left-{size}{unit_name}{suffix}
    {
        margin-left: size * unit_value;
    }

    for media_query_name in media_query_name_list
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .{prefix}margin-left-{size}{unit_name}-{media_query_name}{suffix}
            {
                margin-left: size * unit_value;
            }
        }
    }

// ~~

MakeMarginLeftValueRange( start, step, count, unit_value, unit_name, media_query_name_list = {}, prefix =, suffix = )

    for index in 0 .. count - 1
    {
        size = start + index * step;

        MakeMarginLeftValue( size, unit_value, unit_name, media_query_name_list, prefix, suffix );
    }

// ~~

MakeMarginRightAuto( media_query_name_list = {}, prefix =, suffix = )

    .{prefix}margin-right-auto{suffix}
    {
        margin-right: auto;
    }

    for media_query_name in media_query_name_list
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .{prefix}margin-right-auto-{media_query_name}{suffix}
            {
                margin-right: auto;
            }
        }
    }

// ~~

MakeMarginRightValue( size, unit_value, unit_name, media_query_name_list = {}, prefix =, suffix = )

    .{prefix}margin-right-{size}{unit_name}{suffix}
    {
        margin-right: size * unit_value;
    }

    for media_query_name in media_query_name_list
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .{prefix}margin-right-{size}{unit_name}-{media_query_name}{suffix}
            {
                margin-right: size * unit_value;
            }
        }
    }

// ~~

MakeMarginRightValueRange( start, step, count, unit_value, unit_name, media_query_name_list = {}, prefix =, suffix = )

    for index in 0 .. count - 1
    {
        size = start + index * step;

        MakeMarginRightValue( size, unit_value, unit_name, media_query_name_list, prefix, suffix );
    }

// ~~

MakeFractionalMarginLeftValue( column_count, media_query_name_list = {}, prefix =, suffix = )

    for column_index in 1 .. column_count

        .{prefix}margin-left-{column_index}\\/{column_count}{suffix}
        {
            margin-left: 100% * ( column_index / column_count );
        }

        for media_query_name in media_query_name_list
        {
            @media MediaQueryMap[ media_query_name ]
            {
                .{prefix}margin-left-{column_index}\\/{column_count}-{media_query_name}{suffix}
                {
                    margin-left: 100% * ( column_index / column_count );
                }
            }
        }

// ~~

MakeFractionalMarginLeftValueRange( first_column_count, last_column_count, media_query_name_list = {}, prefix =, suffix = )

    for column_count in first_column_count .. last_column_count
    {
        MakeFractionalMarginLeftValue( column_count, media_query_name_list, prefix, suffix );
    }

// ~~

MakeFractionalMarginRightValue( column_count, media_query_name_list = {}, prefix =, suffix = )

    for column_index in 1 .. column_count

        .{prefix}margin-right-{column_index}\\/{column_count}{suffix}
        {
            margin-right: 100% * ( column_index / column_count );
        }

        for media_query_name in media_query_name_list
        {
            @media MediaQueryMap[ media_query_name ]
            {
                .{prefix}margin-right-{column_index}\\/{column_count}-{media_query_name}{suffix}
                {
                    margin-right: 100% * ( column_index / column_count );
                }
            }
        }

// ~~

MakeFractionalMarginRightRange( first_column_count, last_column_count, media_query_name_list = {}, prefix =, suffix = )

    for column_count in first_column_count .. last_column_count
    {
        MakeFractionalMarginRightValue( column_count, media_query_name_list, prefix, suffix );
    }
