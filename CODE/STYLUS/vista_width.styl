// -- MIXINS

MakeWidthAuto( media_query_name_array = {}, prefix =, suffix =, important = )

    .{prefix}width-auto{suffix}
    {
        width: auto important;
    }

    for media_query_name in media_query_name_array
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .{prefix}width-auto-{media_query_name}{suffix}
            {
                width: auto important;
            }
        }
    }

// ~~

MakeWidthViewport( media_query_name_array = {}, prefix =, suffix =, important = )

    .{prefix}width-viewport{suffix}
    {
        width: 100lvw important;
    }

    for media_query_name in media_query_name_array
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .{prefix}width-viewport-{media_query_name}{suffix}
            {
                width: 100lvw important;
            }
        }
    }

// ~~

MakeWidthValue( size, unit_value, unit_name, media_query_name_array = {}, prefix =, suffix =, important = )

    .{prefix}width-{size}{unit_name}{suffix}
    {
        width: size * unit_value important;
    }

    for media_query_name in media_query_name_array
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .{prefix}width-{size}{unit_name}-{media_query_name}{suffix}
            {
                width: size * unit_value important;
            }
        }
    }

// ~~

MakeWidthValueRange( start, step, count, unit_value, unit_name, media_query_name_array = {}, prefix =, suffix =, important = )

    for index in 0 .. count - 1
    {
        size = start + index * step;

        MakeWidthValue( size, unit_value, unit_name, media_query_name_array, prefix, suffix, important );
    }

// ~~

MakeMinWidthViewport( media_query_name_array = {}, prefix =, suffix =, important = )

    .{prefix}min-width-viewport{suffix}
    {
        min-width: 100lvw important;
    }

    for media_query_name in media_query_name_array
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .{prefix}min-width-viewport-{media_query_name}{suffix}
            {
                min-width: 100lvw important;
            }
        }
    }

// ~~

MakeMinWidthValue( size, unit_value, unit_name, media_query_name_array = {}, prefix =, suffix =, important = )

    .{prefix}min-width-{size}{unit_name}{suffix}
    {
        min-width: size * unit_value important;
    }

    for media_query_name in media_query_name_array
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .{prefix}min-width-{size}{unit_name}-{media_query_name}{suffix}
            {
                min-width: size * unit_value important;
            }
        }
    }

// ~~

MakeMinWidthValueRange( start, step, count, unit_value, unit_name, media_query_name_array = {}, prefix =, suffix =, important = )

    for index in 0 .. count - 1
    {
        size = start + index * step;

        MakeMinWidthValue( size, unit_value, unit_name, media_query_name_array, prefix, suffix, important );
    }

// ~~

MakeMaxWidthViewport( media_query_name_array = {}, prefix =, suffix =, important = )

    .{prefix}max-width-viewport{suffix}
    {
        max-width: 100lvw important;
    }

    for media_query_name in media_query_name_array
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .{prefix}max-width-viewport-{media_query_name}{suffix}
            {
                max-width: 100lvw important;
            }
        }
    }

// ~~

MakeMaxWidthValue( size, unit_value, unit_name, media_query_name_array = {}, prefix =, suffix =, important = )

    .{prefix}max-width-{size}{unit_name}{suffix}
    {
        max-width: size * unit_value important;
    }

    for media_query_name in media_query_name_array
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .{prefix}max-width-{size}{unit_name}-{media_query_name}{suffix}
            {
                max-width: size * unit_value important;
            }
        }
    }

// ~~

MakeMaxWidthValueRange( start, step, count, unit_value, unit_name, media_query_name_array = {}, prefix =, suffix =, important = )

    for index in 0 .. count - 1
    {
        size = start + index * step;

        MakeMaxWidthValue( size, unit_value, unit_name, media_query_name_array, prefix, suffix, important );
    }

// ~~

MakeFractionalWidthValueRange( column_count, media_query_name_array = {}, prefix =, suffix =, important = )

    for column_index in 1 .. column_count
    {
        .{prefix}width-{column_index}\\/{column_count}{suffix}
        {
            width: 100% * ( column_index / column_count ) important;
        }

        for media_query_name in media_query_name_array
        {
            @media MediaQueryMap[ media_query_name ]
            {
                .{prefix}width-{column_index}\\/{column_count}-{media_query_name}{suffix}
                {
                    width: 100% * ( column_index / column_count ) important;
                }
            }
        }
    }

// ~~

MakeMinFractionalWidthValueRange( column_count, media_query_name_array = {}, prefix =, suffix =, important = )

    for column_index in 1 .. column_count
    {
        .{prefix}min-width-{column_index}\\/{column_count}{suffix}
        {
            min-width: 100% * ( column_index / column_count ) important;
        }

        for media_query_name in media_query_name_array
        {
            @media MediaQueryMap[ media_query_name ]
            {
                .{prefix}min-width-{column_index}\\/{column_count}-{media_query_name}{suffix}
                {
                    min-width: 100% * ( column_index / column_count ) important;
                }
            }
        }
    }

// ~~

MakeMaxFractionalWidthValueRange( column_count, media_query_name_array = {}, prefix =, suffix =, important = )

    for column_index in 1 .. column_count
    {
        .{prefix}max-width-{column_index}\\/{column_count}{suffix}
        {
            max-width: 100% * ( column_index / column_count ) important;
        }

        for media_query_name in media_query_name_array
        {
            @media MediaQueryMap[ media_query_name ]
            {
                .{prefix}max-width-{column_index}\\/{column_count}-{media_query_name}{suffix}
                {
                    max-width: 100% * ( column_index / column_count ) important;
                }
            }
        }
    }

// ~~

MakeGridWidthValueRange( column_count, total_size, gutter_size, unit_name, media_query_name_array = {}, prefix =, suffix =, important = )

    column_size = ( total_size - ( column_count - 1 ) * gutter_size ) / column_count;

    for column_index in 1 .. column_count
    {
        .{prefix}grid-width-{column_index}{unit_name}{suffix}
        {
            width: column_size * column_index + gutter_size * ( column_index - 1 ) important;
        }

        for media_query_name in media_query_name_array
        {
            @media MediaQueryMap[ media_query_name ]
            {
                .{prefix}grid-width-{column_index}{unit_name}-{media_query_name}{suffix}
                {
                    width: column_size * column_index + gutter_size * ( column_index - 1 ) important;
                }
            }
        }
    }
