MakeAlignItemsStart( media_query_name_list = {}, suffix = )

    .align-items-start{suffix}
    {
        align-items: flex-start;
    }

    for media_query_name in media_query_name_list
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .align-items-start-{media_query_name}{suffix}
            {
                align-items: flex-start;
            }
        }
    }

// ~~

MakeAlignItemsCenter( media_query_name_list = {}, suffix = )

    .align-items-center{suffix}
    {
        align-items: center;
    }

    for media_query_name in media_query_name_list
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .align-items-center-{media_query_name}{suffix}
            {
                align-items: center;
            }
        }
    }

// ~~

MakeAlignItemsEnd( media_query_name_list = {}, suffix = )

    .align-items-end{suffix}
    {
        align-items: flex-end;
    }

    for media_query_name in media_query_name_list
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .align-items-end-{media_query_name}{suffix}
            {
                align-items: flex-end;
            }
        }
    }

// ~~

MakeAlignItemsStretch( media_query_name_list = {}, suffix = )

    .align-items-stretch{suffix}
    {
        align-items: stretch;
    }

    for media_query_name in media_query_name_list
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .align-items-stretch-{media_query_name}{suffix}
            {
                align-items: stretch;
            }
        }
    }

// ~~

MakeAlignItemsBaseline( media_query_name_list = {}, suffix = )

    .align-items-baseline{suffix}
    {
        align-items: baseline;
    }

    for media_query_name in media_query_name_list
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .align-items-baseline-{media_query_name}{suffix}
            {
                align-items: baseline;
            }
        }
    }

// ~~

MakeAlignSelfStart( media_query_name_list = {}, suffix = )

    .align-self-start{suffix}
    {
        align-self: flex-start;
    }

    for media_query_name in media_query_name_list
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .align-self-start-{media_query_name}{suffix}
            {
                align-self: flex-start;
            }
        }
    }

// ~~

MakeAlignSelfCenter( media_query_name_list = {}, suffix = )

    .align-self-center{suffix}
    {
        align-self: center;
    }

    for media_query_name in media_query_name_list
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .align-self-center-{media_query_name}{suffix}
            {
                align-self: center;
            }
        }
    }

// ~~

MakeAlignSelfEnd( media_query_name_list = {}, suffix = )

    .align-self-end{suffix}
    {
        align-self: flex-end;
    }

    for media_query_name in media_query_name_list
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .align-self-end-{media_query_name}{suffix}
            {
                align-self: flex-end;
            }
        }
    }

// ~~

MakeAlignSelfStretch( media_query_name_list = {}, suffix = )

    .align-self-stretch{suffix}
    {
        align-self: stretch;
    }

    for media_query_name in media_query_name_list
    {
        @media MediaQueryMap[ media_query_name ]
        {

            .align-self-stretch-{media_query_name}{suffix}
            {
                align-self: stretch;
            }
        }
    }

// ~~

MakeAlignSelfBaseline( media_query_name_list = {}, suffix = )

    .align-self-baseline{suffix}
    {
        align-self: baseline;
    }

    for media_query_name in media_query_name_list
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .align-self-baseline-{media_query_name}{suffix}
            {
                align-self: baseline;
            }
        }
    }

