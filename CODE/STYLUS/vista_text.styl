// -- MIXINS

MakeVerticalAlignBottom( media_query_name_array = {}, prefix =, suffix =, important = )

    .{prefix}vertical-align-bottom{suffix}
    {
        vertical-align: bottom important;
    }

    for media_query_name in media_query_name_array
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .{prefix}vertical-align-bottom-{media_query_name}{suffix}
            {
                vertical-align: bottom important;
            }
        }
    }

// ~~

MakeVerticalAlignMiddle( media_query_name_array = {}, prefix =, suffix =, important = )

    .{prefix}vertical-align-middle{suffix}
    {
        vertical-align: middle important;
    }

    for media_query_name in media_query_name_array
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .{prefix}vertical-align-middle-{media_query_name}{suffix}
            {
                vertical-align: middle important;
            }
        }
    }

// ~~

MakeVerticalAlignTop( media_query_name_array = {}, prefix =, suffix =, important = )

    .{prefix}vertical-align-top{suffix}
    {
        vertical-align: top important;
    }

    for media_query_name in media_query_name_array
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .{prefix}vertical-align-top-{media_query_name}{suffix}
            {
                vertical-align: top important;
            }
        }
    }

// ~~

MakeVerticalAlignSub( media_query_name_array = {}, prefix =, suffix =, important = )

    .{prefix}vertical-align-sub{suffix}
    {
        vertical-align: sub important;
    }

    for media_query_name in media_query_name_array
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .{prefix}vertical-align-sub-{media_query_name}{suffix}
            {
                vertical-align: sub important;
            }
        }
    }

// ~~

MakeVerticalAlignBaseline( media_query_name_array = {}, prefix =, suffix =, important = )

    .{prefix}vertical-align-baseline{suffix}
    {
        vertical-align: baseline important;
    }

    for media_query_name in media_query_name_array
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .{prefix}vertical-align-baseline-{media_query_name}{suffix}
            {
                vertical-align: baseline important;
            }
        }
    }

// ~~

MakeVerticalAlignTextTop( media_query_name_array = {}, prefix =, suffix =, important = )

    .{prefix}vertical-align-text-top{suffix}
    {
        vertical-align: text-top important;
    }

    for media_query_name in media_query_name_array
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .{prefix}vertical-align-text-top-{media_query_name}{suffix}
            {
                vertical-align: text-top important;
            }
        }
    }

// ~~

MakeTextAlignLeft( media_query_name_array = {}, prefix =, suffix =, important = )

    .{prefix}text-align-left{suffix}
    {
        text-align: left important;
    }

    for media_query_name in media_query_name_array
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .{prefix}text-align-left-{media_query_name}{suffix}
            {
                text-align: left important;
            }
        }
    }

// ~~

MakeTextAlignCenter( media_query_name_array = {}, prefix =, suffix =, important = )

    .{prefix}text-align-center{suffix}
    {
        text-align: center important;
    }

    for media_query_name in media_query_name_array
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .{prefix}text-align-center-{media_query_name}{suffix}
            {
                text-align: center important;
            }
        }
    }

// ~~

MakeTextAlignRight( media_query_name_array = {}, prefix =, suffix =, important = )

    .{prefix}text-align-right{suffix}
    {
        text-align: right important;
    }

    for media_query_name in media_query_name_array
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .{prefix}text-align-right-{media_query_name}{suffix}
            {
                text-align: right important;
            }

            .{prefix}text-align-justify-{media_query_name}{suffix}
            {
                text-align: justify important;
            }
        }
    }

// ~~

MakeTextAlignJustify( media_query_name_array = {}, prefix =, suffix =, important = )

    .{prefix}text-align-justify{suffix}
    {
        text-align: justify important;
    }

    for media_query_name in media_query_name_array
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .{prefix}text-align-justify-{media_query_name}{suffix}
            {
                text-align: justify important;
            }
        }
    }

// ~~

MakeTextTransformLowercase( media_query_name_array = {}, prefix =, suffix =, important = )

    .{prefix}text-transform-lowercase{suffix}
    {
        text-transform: lowercase important;
    }

    for media_query_name in media_query_name_array
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .{prefix}text-transform-lowercase-{media_query_name}{suffix}
            {
                text-transform: lowercase important;
            }
        }
    }

// ~~

MakeTextTransformUppercase( media_query_name_array = {}, prefix =, suffix =, important = )

    .{prefix}text-transform-uppercase{suffix}
    {
        text-transform: uppercase important;
    }

    for media_query_name in media_query_name_array
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .{prefix}text-transform-uppercase-{media_query_name}{suffix}
            {
                text-transform: uppercase important;
            }
        }
    }

// ~~

MakeTextTransformCapitalize( media_query_name_array = {}, prefix =, suffix =, important = )

    .{prefix}text-transform-capitalize{suffix}
    {
        text-transform: capitalize important;
    }

    for media_query_name in media_query_name_array
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .{prefix}text-transform-capitalize-{media_query_name}{suffix}
            {
                text-transform: capitalize important;
            }
        }
    }
