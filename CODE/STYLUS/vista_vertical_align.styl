MakeVerticalAlignBottom( media_query_name_list = {}, suffix = )

    .vertical-align-bottom{suffix}
    {
        vertical-align: bottom;
    }

    for media_query_name in media_query_name_list
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .vertical-align-bottom-{media_query_name}{suffix}
            {
                vertical-align: bottom;
            }
        }
    }

// ~~

MakeVerticalAlignMiddle( media_query_name_list = {}, suffix = )

    .vertical-align-middle{suffix}
    {
        vertical-align: middle;
    }

    for media_query_name in media_query_name_list
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .vertical-align-middle-{media_query_name}{suffix}
            {
                vertical-align: middle;
            }
        }
    }

// ~~

MakeVerticalAlignTop( media_query_name_list = {}, suffix = )

    .vertical-align-top{suffix}
    {
        vertical-align: top;
    }

    for media_query_name in media_query_name_list
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .vertical-align-top-{media_query_name}{suffix}
            {
                vertical-align: top;
            }
        }
    }

// ~~

MakeVerticalAlignSub( media_query_name_list = {}, suffix = )

    .vertical-align-sub{suffix}
    {
        vertical-align: sub;
    }

    for media_query_name in media_query_name_list
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .vertical-align-sub-{media_query_name}{suffix}
            {
                vertical-align: sub;
            }
        }
    }

// ~~

MakeVerticalAlignBaseline( media_query_name_list = {}, suffix = )

    .vertical-align-baseline{suffix}
    {
        vertical-align: baseline;
    }

    for media_query_name in media_query_name_list
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .vertical-align-baseline-{media_query_name}{suffix}
            {
                vertical-align: baseline;
            }
        }
    }

// ~~

MakeVerticalAlignTextTop( media_query_name_list = {}, suffix = )

    .vertical-align-text-top{suffix}
    {
        vertical-align: text-top;
    }

    for media_query_name in media_query_name_list
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .vertical-align-text-top-{media_query_name}{suffix}
            {
                vertical-align: text-top;
            }
        }
    }
