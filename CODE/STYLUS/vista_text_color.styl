MakeTextColorValue( name, value, media_query_name_list = {}, prefix =, suffix = )

    .{prefix}text-color-{name}{suffix}
    {
        color: value;
    }

    for media_query_name in media_query_name_list
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .{prefix}text-color-{name}-{media_query_name}{suffix}
            {
                color: value;
            }
        }
    }
