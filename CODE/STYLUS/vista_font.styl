MakeFontSizeValue( size, unit_value, unit_name, media_query_name_list = {} )

    .font-size-{size}{unit_name}
    {
        font-size: size * unit_value;
    }

    for media_query_name in media_query_name_list
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .font-size-{size}{unit_name}-{media_query_name}
            {
                font-size: size * unit_value;
            }
        }
    }

// ~~

MakeFontSizeValueRange( start, step, count, unit_value, unit_name, media_query_name_list = {} )

    for index in 0 .. count - 1
    {
        size = start + index * step;

        MakeFontSizeValue( size, unit_value, unit_name, media_query_name_list );
    }

// ~~

MakeFontWeightValue( weight, media_query_name_list = {} )

    .font-weight-{weight}
    {
        font-weight: weight;
    }

    for media_query_name in media_query_name_list
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .font-weight-{weight}-{media_query_name}
            {
                font-weight: weight;
            }
        }
    }

// ~~

MakeFontWeightValueRange( start, step, count, media_query_name_list = {} )

    for index in 0 .. count - 1
    {
        weight = start + index * step;

        MakeFontWeightValue( weight, media_query_name_list );
    }
