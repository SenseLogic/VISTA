MakeTextAlignLeft( media_query_name_list = {}, suffix = )

    .text-align-left{suffix}
    {
        text-align: left;
    }

    for media_query_name in media_query_name_list
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .text-align-left-{media_query_name}{suffix}
            {
                text-align: left;
            }
        }
    }

// ~~

MakeTextAlignCenter( media_query_name_list = {}, suffix = )

    .text-align-center{suffix}
    {
        text-align: center;
    }

    for media_query_name in media_query_name_list
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .text-align-center-{media_query_name}{suffix}
            {
                text-align: center;
            }
        }
    }

// ~~

MakeTextAlignRight( media_query_name_list = {}, suffix = )

    .text-align-right{suffix}
    {
        text-align: right;
    }

    for media_query_name in media_query_name_list
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .text-align-right-{media_query_name}{suffix}
            {
                text-align: right;
            }

            .text-align-justify-{media_query_name}{suffix}
            {
                text-align: justify;
            }
        }
    }

// ~~

MakeTextAlignJustify( media_query_name_list = {}, suffix = )

    .text-align-justify{suffix}
    {
        text-align: justify;
    }

    for media_query_name in media_query_name_list
    {
        @media MediaQueryMap[ media_query_name ]
        {
            .text-align-justify-{media_query_name}{suffix}
            {
                text-align: justify;
            }
        }
    }
