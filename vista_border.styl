.border-debug,
.border-debug *
{
    border: 0.1rem solid rgb( 255, 0, 0, 0.5 );
}

// ~~

MakeBorderRadius( size, unit_value, unit_name, media_query_name_list, media_query_map )

    .border-radius-{size}{unit_name}
    {
        border-radius: size * unit_value;
    }

    for media_query_name in media_query_name_list
    {
        @media media_query_map[ media_query_name ]
        {
            .border-radius-{size}{unit_name}-{media_query_name}
            {
                border-radius: size * unit_value;
            }
        }
    }

// ~~

MakeBorderRadiusRange( start, step, count, unit_value, unit_name, media_query_name_list, media_query_map )

    for index in 0 .. count
    {
        size = start + index * step;

        MakeBorderRadius( size, unit_value, unit_name, media_query_name_list, media_query_map );
    }

// ~~

MakeBorderColor( name, value, media_query_name_list, media_query_map )

    .border-color-{name}
    {
        border-color: value;
    }

    for media_query_name in media_query_name_list
    {
        @media media_query_map[ media_query_name ]
        {
            .border-color-{name}-{media_query_name}
            {
                border-color: value;
            }
        }
    }
